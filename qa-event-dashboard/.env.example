# Dashboard access password.
# Set this to protect the dashboard with a password gate.
# Copy to .env.local for local development, or add to the CI environment for deployments.
# If left unset, the dashboard is accessible without a password (local dev default).
NEXT_PUBLIC_DASHBOARD_PASSWORD=your-password-here
